services:
  - docker

before_install:
  - docker build -t hello-world .
  - docker run -d -p 127.0.0.1:8000:8000 hello-world

script:
  - docker ps -a
  - curl localhost:8000
  - curl localhost:8000/health
