sudo: required

install: true

services:
  - docker

install: true

script: true

after_success:
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
  - docker --version
  - ./deploy.sh

language: go

